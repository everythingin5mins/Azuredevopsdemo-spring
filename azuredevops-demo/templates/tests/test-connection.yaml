apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "azuredevops-demo.fullname" . }}-test-connection"
  labels:
    {{- include "azuredevops-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "azuredevops-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
