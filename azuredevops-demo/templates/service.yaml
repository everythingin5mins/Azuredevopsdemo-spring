apiVersion: v1
kind: Service
metadata:
  name: {{ include "azuredevops-demo.fullname" . }}
  labels:
    {{- include "azuredevops-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "azuredevops-demo.selectorLabels" . | nindent 4 }}
