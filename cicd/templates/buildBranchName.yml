        steps:
          - bash: |
              echo "##vso[build.updatebuildnumber]PullRequest-$(major).$(minor).$(branchPRNumber)"
            condition: eq(variables['Build.Reason'], 'PullRequest')
            name: 'UpdatePRBranch'
          - bash: |
              echo "##vso[build.updatebuildnumber]$(buildName)-$(major).$(minor).$(branchBuildNumber)"
            condition: eq(variables['Build.SourceBranch'], 'refs/heads/master')
            name: 'UpdateBuildBranch'
          